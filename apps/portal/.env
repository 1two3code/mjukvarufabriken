VITE_APP_VERSION=$npm_package_version
VITE_API_URL=/bff
VITE_APP_TITLE="Mjukvaruhuset portal"
# SaaS Sentry (free tier) DSN — empty until a Sentry project exists (TODO-EXTERNAL); Sentry.init
# no-ops cleanly on an empty dsn, so this is not a security gap, just an unset feature.
VITE_SENTRY_DSN=
