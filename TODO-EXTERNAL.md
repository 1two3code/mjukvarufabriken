# Things that need other people's approval — off the critical path

Start these NOW; none of them block building the software.

| Item | Who | Typical wait | Blocks |
|---|---|---|---|
| Register AB at Bolagsverket (verksamt.se), 25k SEK share capital | you | 1–3 weeks | invoicing in company name |
| F-skatt + momsregistrering (Skatteverket) | you | 1–3 weeks | invoicing |
| Company bank account | you | 1–4 weeks | Stripe payouts |
| Stripe live-mode verification (needs org.nr + bank) | you | 1–5 days after above | real payments |
| Fortnox (or similar) for bokföring + Stripe sync | you | 1 day | accounting |
| Ansvarsförsäkring (IT-konsult) e.g. Länsförsäkringar/If/Gjensidige | you | days | pilots with real customers |
| Lawyer review of kundavtal, PUB-avtal (GDPR), SLA, IP/liability clause — drafts in `legal/` since 2026-08-27 (`kundavtal.md`, `pub-avtal.md`, `sla-resident.md`, `villkor-webb.md`, each marked DRAFT — EJ GRANSKAD with an English summary); fill in `[BOLAGSNAMN AB]`/`[ORG.NR]`/`[ADRESS]`/`[MÅNADSAVGIFT]` and answer the bracketed open points (resident fee model: is "tokens × 1,5" the customer's total model cost — 0,5 × list to us on the customer's key — or 1,5 × list to us on top of the customer's Anthropic bill; Anthropic retention; licence gate; log retention; deletion of supplier copies is manual today) before sending. Review fixes 2026-08-27: balance due at acceptance (payment ≠ acceptance), retry before budget cancellation, cancellation matched to the order states, 12-month copy retention in both kundavtal §7.3 and PUB §10.2, GitHub sign-in removed everywhere until built (M6); `apps/api/test/legal/drafts.test.ts` guards the facts | lawyer | 1–2 weeks | pilots |
| Anthropic: raise rate/spend limits on API org | Anthropic | days | volume |
| AWS: service quota increases (Fargate vCPU; ECS Express Mode services) | AWS | 1–3 days | parallel jobs / customer hosting |
| AWS SES production access (leave sandbox) | AWS | ~1 day | customer email |
| Domain mjukvaruhuset.se — check availability, buy | registrar | hours | site live |
| GitHub OAuth App for "Sign in with GitHub" (one per env; callback `https://portal.<env>.mjukvaruhuset.se/auth/github/callback`, locally `http://localhost:5173/auth/github/callback`) under the `mjukvaruhuset` org. Then: client id → `infra/lib/config.ts` `githubOAuth.clientId` (locally `GITHUB_OAUTH_CLIENT_ID`), client secret → the `mf/<env>/github-oauth-client-secret` secret (locally `GITHUB_OAUTH_CLIENT_SECRET`), and `VITE_GITHUB_SIGNIN=1` in `apps/portal/.env.dev` (+ `.env` for live) so the button shows. Until then the api answers 404 on `/bff/auth/github` and the magic link is the only way in (code built + tested 2026-08-27, wave 4) | you | minutes (needs the org first) | M6 GitHub sign-in |
| BankID integration (via a broker e.g. Criipto/Signicat) | broker | weeks | later, not v1 |
| Trademark check on "Mjukvaruhuset" (PRV) | you | days | brand risk |
| Hard network egress fence for build jobs: Fargate sidecars share the task ENI, so the tinyproxy allowlist is app-level (HTTPS_PROXY). A proxy in its own task/SG + VPC endpoints for Secrets Manager/S3 gives a real fence (~10 USD/month) — decide before pilots | you | hours | untrusted-code isolation |
| Anthropic: Claude Agent SDK usage terms / org rate limits for parallel workers (2–4 sessions per job) | Anthropic | days | parallel jobs |
| Mailbox hej@mjukvaruhuset.se (shown on the public site as contact address; contact-form mail goes to AUTH_ADMIN_EMAILS via SES) | you | hours | site contact address answering |
| Confirm the `mf-alerts-<env>` SNS e-mail subscription (AWS sends "Subscription Confirmation" to each `adminEmails` address after the first `ops-<env>` deploy; nothing is delivered until clicked) | you | minutes | M9 alerts reaching anyone |
| GitHub delivery: create the **`mjukvaruhuset` GitHub org** and install the delivery App on it. DONE 2026-08-28: App (id 4746145, install 157166357) auth wired + verified — mints installation tokens, perms `administration:write`+`contents:write`+`emails:read`, key in `mf/dev/github-app-key`, deployed. REMAINING: the App is installed on the personal `1two3code` account, but delivery creates repos in the `mjukvaruhuset` org (`createInOrg`) which doesn't exist — so create that org + install the App there (delivered repos live there, then transfer to the customer). Until then delivery fails closed at `createRepo` (dev is `DELIVERY_DRY_RUN`) | you | minutes | M5 live repo push |
| ~~App Runner GitHub connection~~ — NO LONGER NEEDED (2026-08-28): hosting moved to ECS Express Mode, which takes a prebuilt ECR image (built by CodeBuild), not a GitHub source connection. Nothing external required for hosting; the customer GitHub org/token is still needed for the repo *transfer* | — | — | (removed) |
| Preview identity provider: the delivered api refuses to boot without `AUTH_ISSUER` + `AUTH_JWKS_URL`; the job reads `PREVIEW_AUTH_ISSUER` (our api URL, which publishes `/.well-known/jwks.json`) — set `auth.issuer` per env in `infra/lib/config.ts` (needs the api custom domain); without it the deploy step is skipped with `deployUrl: null` | you | minutes | M5 preview URL |
| GitHub App for the `mjukvaruhuset` org so build jobs mint short-lived per-job installation tokens instead of holding the org token (docs/M3-REVIEW / M9 wanted this; v1 grants the job task role read on `github-token`) | you | hour | job sandbox hardening |
| ACM certificate + `domain` config for `live` (today only `dev` has one): without it the api ALB is plain http and build jobs report over cleartext (`cdk synth` warns `mf:job-api-url-http`, the api logs a warning at start) | you | hours (DNS validation) | job reports over TLS in live |
| ~~Decide on a second uid for worker sessions in the job image~~ — done 2026-08-27 (wave 5 `sandbox-uid`): `worker` uid 1001 via `setpriv`, own HOME, shared `/work` group (apps/job/README.md "Sandbox uid"). Left for you: run one dev job on Fargate and confirm the task starts (the task definition must not drop `SETUID`/`SETGID` — it does not today) and that `docker exec -u worker … cat /proc/1/environ` is refused | you | 30 min | untrusted-code isolation |
| Activate the `Environment` cost-allocation tag (Billing → Cost allocation tags → user-defined → activate; takes up to 24 h) so the per-env AWS Budget sees spend instead of 0 | you | 1 day | M9 budget alerts |
| Stripe test-mode keys (Dashboard → Developers → API keys + a webhook endpoint `https://api.<env>.mjukvaruhuset.se/bff/stripe/webhook` for `checkout.session.completed`, `checkout.session.async_payment_succeeded` and `checkout.session.async_payment_failed`): put `sk_test_…` in the `stripe-secret-key` secret and `whsec_…` in `stripe-webhook-secret` (or `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` locally). Until then the api runs the clearly-labelled fake provider (no money moves) | you | minutes | M6 Stripe Checkout in test mode |
| Resident pilot (M8): a customer account to `cdk deploy` `infra/resident` into (their Anthropic key, a fine-grained GitHub token limited to the one repo with contents + issues + pull-requests write), and per installation an entry in the api's `RESIDENT_INSTALLATIONS` secret (`id:token`) so its daily usage records are accepted | you | hours | M8 resident in a real account |
| Stripe usage-based billing for the resident (M8): in the Stripe dashboard create a **billing meter** with `event_name` `resident_usage_usd_cents` (customer mapping `stripe_customer_id`, value `value`, aggregation sum), a USD **metered price** on that meter at 0.01 USD per unit (plus a flat monthly-fee price), subscribe each pilot customer to both, then per installation `PUT /bff/admin/resident/installations/:id { orgId, billingCustomerId: "cus_…" }` and run `POST /bff/admin/resident/usage/<YYYY-MM>/bill` after month end. Optional env: `RESIDENT_USAGE_METER_EVENT` (if the meter is named differently), `RESIDENT_USAGE_PRICE_ID` (informational) — neither is wired into the CDK web stack yet | you | 30 min | M8 resident billing (first real invoice) |
| ~~Deploy role~~ — DONE for dev 2026-08-30: `mf-github-deploy` role + GitHub OIDC provider are the infra `github-deploy` stack (deployed once per account; trusts `repo:1two3code/mjukvarufabriken:environment:{dev,qa,live}`, may only assume the CDK bootstrap roles + describe CDKToolkit), and the `dev` GitHub environment has `AWS_REGION`/`AWS_ACCOUNT_ID` vars + the `AWS_DEPLOY_ROLE_ARN` secret. REMAINING for qa/live: create those GitHub environments with the same three values (same role ARN; live with a required reviewer), then re-chain them onto push in deploy.yml. Historical note: the role needs `cloudformation:DescribeStacks` on `CDKToolkit` in eu-north-1 and us-east-1 (it has it), and a new account/region must be bootstrapped by hand (`npx cdk bootstrap aws://<account>/us-east-1`, admin credentials) or give the role the one-time bootstrap rights (CloudFormation + S3 + ECR + IAM for `CDKToolkit`). Without it the first push to `main` after wave 5 fails at the "CDK bootstrap (if needed)" step before deploying anything | you | minutes | `budget-<env>` in the pipeline (M9), every deploy after wave 5 |

<!-- M5 live-delivery dev-notes (not external, tracked here for visibility):
- ECS Express deploy client + CodeBuild image build are code-complete + dry-run/fake verified, infra deployed to dev, but LIVE-UNVERIFIED (post-cutoff API; no org yet). Before the first real deploy: wire the per-job CodeBuild source (upload the job's repo zip → sourceLocationOverride, or a GitHub-source project), then run one real delivery once the mjukvaruhuset org exists. -->
| ~~Catch-all inbound mail on mjukvaruhuset.se~~ — DONE 2026-08-30: `infra/mail` deployed (`mf-mail` stack, eu-north-1). MX live (`dig MX mjukvaruhuset.se` → `inbound-smtp.eu-north-1.amazonaws.com`), receipt rule set active, live-verified end-to-end (a test send to `test@mjukvaruhuset.se` landed forwarded). Fixed on deploy: the forwarder's IAM grant needed `ses:SendRawEmail` (not `ses:SendEmail`) with `Resource: '*'` + a `ses:FromAddress` condition — SES also resource-checks the *recipient's* identity ARN when the recipient is itself verified in-account, so a policy scoped only to the sender identity 403s. `hasse.lofgren@outlook.com` was verified as an SES identity to satisfy the sandbox's "recipient must be verified" rule too — **drop that identity once SES production access below clears the sandbox**, it stops being necessary | — | done | M11 / org account vending |
| Deploy `infra/status` (Uptime Kuma status page for status.mjukvaruhuset.se): synth-verified offline against checked-in PENDING placeholders (`infra/status/lib/config.ts`), not deployed. Before `npx cdk deploy`: (1) issue an ACM cert — `aws acm request-certificate --domain-name status.mjukvaruhuset.se --validation-method DNS --region eu-north-1`, DNS-validate in the hosted zone, pass the ARN via `-c certificateArn=...`; (2) reuse the dev environment's VPC — `aws cloudformation describe-stacks --stack-name resources-dev --query "Stacks[0].Outputs"` for the VPC id, `aws ec2 describe-subnets --filters Name=vpc-id,Values=<id> Name=tag:aws-cdk:subnet-type,Values=Public` for its public subnet ids, pass via `-c vpcId=... -c availabilityZones=... -c publicSubnetIds=...`. After deploy: open `https://status.mjukvaruhuset.se`, complete Uptime Kuma's first-run setup (admin user), then add the 3 monitors by hand (site/portal/api dev URLs) — no CDK-friendly config API for that | you | ~1 hour | public status page live |
| Confirm the hardcoded Anthropic per-model list prices behind resident billing (2026-08-28, wave 7 s6): `cost()` in `packages/harness/src/job/types.ts` (`modelPrices`) bills each token bucket at its own USD/MTok rate — opus 15/75, sonnet 3/15, haiku 1/5, 3-5-haiku 0.8/4, cache-read 0.1×, cache-write 1.25×. These were captured from anthropic.com/pricing on 2026-08-28; verify the exact current rates per model id against the Anthropic console and adjust before they drive real invoices — since 2026-08-30 build-job pricing reads the `model_prices` table, editable in the admin page ("Model prices": add a row, it applies to orders created from its effective date); resident metering still uses the built-in table / `RESIDENT_PRICES_JSON` override. Reconcile the first resident month's `billableUsd` against the console total | you | 30 min | resident billing accuracy (real invoices) |
