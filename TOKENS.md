# Token ledger — append `/cost` per session

Format: date | milestone | session tokens (in/out) | notes

| date | milestone | in | out | notes |
|---|---|---|---|---|
| 2026-08-26 | planning | ~25k | ~5k | plan + lists written |
| 2026-08-26 | M1 template extraction | ~300k | ~30k | subagent, 61 tool calls, 21 min; lint+68 tests green |
| 2026-08-26 | M1 template infra + CI | ~320k | ~30k | subagent follow-up: resources stack, GitHub Actions |
| 2026-08-26 | M1 monorepo scaffold | ~130k | ~25k | subagent: apps/site+portal+api, packages ×6, infra (2 SPAs), CI; lint+68 tests+build+synth green |
| 2026-08-26 | M1 monorepo scaffold | ~100k | ~10k | subagent: 8 workspaces, 68 tests, 4 stacks synth; CLAUDE.md dedup |
| 2026-08-26 | M1 CDK stack | ~90k | ~15k | subagent: RDS Postgres 17, secrets placeholders, artifacts bucket, mf-jobs cluster + task def, api wiring; 4 stacks synth offline, lint + 68 tests green |
| 2026-08-26 | M1 deploy dev | ~60k | ~6k | bootstrap, resources-dev + mf-dev live; Dockerfile --ignore-scripts fix |
| 2026-08-26 | M1 custom domains | ~45k | ~5k | ACM certs, Route53 records, CloudFront /bff→ALB, dev on mjukvaruhuset.se |
| 2026-08-26 | M1 prod-bundle fix | ~40k | ~5k | sessionSlice import cycle broke SPAs in prod; fixed in site/portal/template, verified headless |
| 2026-08-26 | M2 spec engine | ~190k | ~35k | subagent: models Spec schema, harness engine + estimator (17 tests), api plugins/service/routes (+17 tests → 73), portal spec page sv/en; lint + 110 tests + build + smoke green; live model unverified (key empty) |
| 2026-08-26 | M2 spec engine | ~190k | ~25k | subagent: models/harness/api/portal, 110 tests; infra secret wiring + dev deploy |
| 2026-08-26 | M6 magic-link auth (pulled forward) | ~250k | ~45k | subagent: models User/Org/Auth.api, api authKeys/email/auth plugins + user/auth services + 5 routes (123 api tests, 160 total), portal login/callback/header sv+en, infra secret + SES + config; lint + build + smoke + synth green |
| 2026-08-26 | M3 orchestrator + sandbox | ~950k | ~110k | subagent ×3 resumes (143+155+165 tool calls, ~93 min): @mf/db postgres driver + migrations, harness plan/DAG/worker/merge/budget/kill (52 tests), apps/job + egress proxy (verified in compose), api job service/routes (232 tests total), portal job page, infra job image + sidecar deployed to dev. Demo jobs: 4 runs, ~1.24M Anthropic tokens spent, none completed a task (400k budget too small; #4 interrupted). Fargate run not done. |
| 2026-08-26 | M3 wrap-up (main session) | ~60k | ~8k | auth verified on dev, M3 brief, stack wait, PLAN corrections |
| 2026-08-26 | M3 review (ultracode workflow) | ~830k | ~60k | 25 agents (5 area reviewers + 1 refuter per finding), 4 min; 20 confirmed / 0 refuted → docs/M3-REVIEW.md |
| 2026-08-26 | M3 review fixes | ~175k | ~30k | subagent: 18 findings fixed in 5 commits, tests 235 → 251, 2 deferred (#12 TLS verify-full → M9, #18 job↔RDS creds → M4 hardening) |
| 2026-08-26 | M3 live verification (main session) | ~120k | ~15k | local demo job #5: 2 of 3 tasks merged, budget abort at 2.007M (≈ USD 5); RDS TLS fix; non-root job image; budgets S 6M / M 15M / L 40M; 3 dev deploys; Fargate run reached planning + fail-closed |
| 2026-08-27 | overnight wave 1 persistence | subagent | n/a | migration 0004 (orders carry SpecDraft, magic_links, refresh_tokens), @mf/db postgres + memory repositories (orders/users/auth/jobs), api db plugin fails closed on unresolvable secret; tests 258 → 268 |
| 2026-08-27 | overnight wave 1 m4-gates | subagent | n/a | Job/Gate schemas, harness runGates (acceptance-tests, review, acceptance-check, fail closed + notify), gate sessions, gates-demo script, migration 0005 jobs.gates/gate_waivers; tests → 312; live run pending |
| 2026-08-27 | overnight wave 1 m7-site | subagent | n/a | apps/site sv/en pages (landing, how it works, pricing, contact, sitemap) + POST /bff/contact with XFF-safe rate limit; tests → 353; site smoke renders 6.9k chars |
| 2026-08-27 | overnight wave 1 m9-ops | subagent | n/a | ops-<env> stack: SNS alerts, 9 alarms, AWS Budgets; RDS backups 7/30 d, bucket versioning; deploy workflow/script deploy ops stack; RUNBOOK; infra tests 16; merge conflicts in PLAN.md/TODO-EXTERNAL.md resolved (kept both) |
| 2026-08-27 | overnight wave 1 (workflow) | ~1.84M | n/a | 21 agents / 33 min: 4 builders + 12 reviewers + 4 fixers + merge; 251 → 353 tests; persistence, M4 gates, site, ops merged |
| 2026-08-27 | overnight main session | ~150k | ~20k | briefs, Fargate runs #6/#7 (≈ USD 13 Anthropic), deps-sync + hooks fixes, deploys |
| 2026-08-27 | overnight wave 2 m5-delivery | subagent | n/a | harness delivery step (docs/repo/deploy/bundle behind GitHubClient/DeployClient/ArtifactStore fakes + dry-run, 23 tests), Deliverable model + delivery event, api s3 plugin + GET /bff/jobs/:id/deliverables (presigned 15 min), job wiring, infra job role (github-token back, App Runner + PassRole on empty instance role); delivery-demo dry run on a scratch repo; live delivery pending GitHub org + App Runner connection |
| 2026-08-27 | overnight wave 2 m6-orders | subagent | n/a | Order/Payment models + state machine, migration 0006, stripe paymentProvider (fake without keys), orderService/paymentService, POST/GET /bff/orders + checkout + webhook (idempotent, webhook id forgotten on failed apply), portal order flow/job page/admin; 12 review findings fixed; tests → 430 in worktree; Stripe test mode pending keys (TODO-EXTERNAL) |
| 2026-08-27 | overnight wave 2 m3-hardening | subagent | n/a | per-job report token (0007) + one-shot exchange, /internal/jobs/:id get/events/patch (forward-only status, killed-guard, idempotent seq events 0008, notify → admin mail, gate → jobs.gates), JobReporter api/db in apps/job, DB grant + 5432 rules removed, JOB_API_URL/JOB_NO_PROXY on api; 5 review fixes; tests → 406 in worktree; live Fargate run pending |
| 2026-08-27 | overnight wave 2 (merge) | subagent | n/a | m5-delivery + m6-orders merged clean; m3-hardening: 12 conflicts (jobService/mocks/db plugin/job app/resources-stack/security test/memory/models index/docs) reconciled — delivery grants (github-token, App Runner, PassRole) and per-job report token both survive, job task role has no DB grant; portal Deliverables aligned with DeliverablesResponse; lint/test 81 files 518 tests/build/smoke/synth/infra 20 green |
| 2026-08-27 | overnight wave 3 m8-resident | subagent | n/a | packages/resident + infra/resident: issues->PRs via harness, monthly token cap, pause/kill switch, audit log, metering to api; 2 fix commits (meter poisoning, stranded tasks) |
| 2026-08-27 | overnight wave 3 efficiency | subagent | n/a | harness worker: JOB_TOKEN stripped from sandbox env, scoped task gate widened on out-of-scope changes, turn caps per size class, WORKER_EFFORT knob; estimates only, no live model calls |
| 2026-08-27 | overnight wave 2 (workflow) | ~2.12M | n/a | 16 agents / 53 min: M5 delivery (dry-run), M6 orders/Stripe(fake)/portal/admin, job→api reporting with per-job token; 353 → 518 tests |
| 2026-08-27 | overnight wave 3 (workflow) | ~1.12M | n/a | 11 agents / 48 min: @mf/resident + infra/resident, worker efficiency (scoped gates, turn caps); 518 → 586 tests |
| 2026-08-27 | overnight wave 4 github-signin | subagent | n/a | Sign in with GitHub: OAuth routes + state cookie, account link by verified email, users.github_id/login (0011), magic_links.purpose so login links skip the rate limit, portal button (hidden until VITE_GITHUB_SIGNIN=1), infra secret placeholder; 3 fix commits; no real OAuth App yet (TODO-EXTERNAL) |
| 2026-08-27 | overnight wave 4 billing-and-tls | subagent | n/a | TLS verify-full to RDS (pinned CA bundle in api/job images), resident usage persistence + installations + monthly reports (0009), Stripe meter billing with reserve-then-confirm and admin endpoints; 2 fix commits; not verified live (PLAN M8 box 3 unticked) |
| 2026-08-27 | overnight wave 4 api-hygiene | subagent | n/a | db-backed contact rate limiter (rate_limits 0010) with process-local fallback, hourly jittered housekeeping, specService.get 404, memory auth sweep, ci for infra/resident; 4 fix commits |
| 2026-08-27 | overnight wave 4 legal-drafts | subagent | n/a | legal/ kundavtal, pub-avtal, sla-resident, villkor-webb (DRAFT — EJ GRANSKAD) + drafts-vs-code test; 1 fix commit; needs lawyer review (PLAN M10 untouched) |
| 2026-08-27 | overnight wave 4 (merge) | subagent | n/a | 4 branches merged --no-ff in order; conflicts: secrets.ts(+mock) github vs billing (both kept), api-hygiene vs resident in db plugin/mock, memory/repositories/index (both kept, closers restored), authService.test import (pruneIntervalMs gone); legal test asserted no GitHub login → villkor-webb now describes it; migrations 0009/0010/0011 unique; lint/test 102 files 677 tests/build/smoke/synth/resident synth green |
| 2026-08-27 | overnight wave 5 sandbox-uid | subagent | n/a | worker uid sandbox (setpriv, +kill cap); merged clean, 701 tests green |
| 2026-08-27 | overnight wave 5 licence-gate | subagent | n/a | licence gate + THIRD-PARTY-LICENCES.md; merged clean, 786 tests green |
| 2026-08-27 | overnight wave 5 portal-polish | subagent | n/a | legal pages, /admin/resident, portal vitest; merged clean, 818 tests green; integration fix: licence gate label sv/en |
| 2026-08-27 | overnight wave 5 ci-and-deploy | subagent | n/a | coverage threshold, bootstrap guard, budget stack in deploy; merged clean, infra 23 tests green |
| 2026-08-27 | overnight wave 4 (workflow) | ~1.99M | n/a | 21 agents / 45 min: GitHub sign-in (env-gated), resident usage billing + RDS verify-full, db-backed rate limits + prune, legal drafts; 586 → 677 tests |
| 2026-08-27 | overnight wave 5 (workflow) | ~1.82M | n/a | 21 agents / 48 min: two-uid sandbox, licence gate, portal legal/admin/order polish + route smoke, CI/deploy bootstrap guard + README; 677 → 818 tests |
| 2026-08-27 | overnight main session | ~250k | ~35k | budget stack → us-east-1 + bootstrap, parameter-property boot crash fix + lint rule, 4 deploys, worktree cleanup |
| 2026-08-27 | overnight main session (morning) | ~200k | ~25k | Fargate runs #8–#9 (each found one bug: empty-JSON token claim, SDK max-turns thrown as error), reporter + turn-cap fixes, 3 job-image deploys, memory + PLAN liveness item |
| 2026-08-27 | overnight main session (afternoon) | ~180k | ~25k | Fargate runs #10–#13 (setpriv caps on fetch, worker-uid npm install, repair staging, CLI json-schema $schema) — #13 built 3 tasks, verify + acceptance-tests gates green; ~USD 30 Anthropic across runs #8–#13; M3 box 2 ticked |
| 2026-08-27 | first green delivery | ~200k | ~30k | fixed licence gate (own private workspaces, ELSPROBLEMS) + planner retry tool_result 400; job 5e894e2a DELIVERED end-to-end — all 5 gates green (review auto-fixed), repo+deploy dry-run, bundle in S3; ~2.5M budget-tokens ≈ USD 6 |
| 2026-08-28 | overnight wave 6 test-and-replay | subagent | n/a | expanded offline e2e (11 cases: kill/budget/two-uid sandbox/delivery-failure debug bundle/App Runner dry-run) + record/replay cassette (@mf/harness/testing) + replay e2e/fixture + e2e:replay + docs/TESTING; merged clean, no conflicts |
| 2026-08-28 | overnight wave 6 api-hygiene | subagent | n/a | background pruner plugin (hourly jittered, Postgres-only) calling pruneExpired() on auth + rate-limit repos, rateLimitRetentionMs invariant in code, one summary log line; merged clean, no conflicts |
| 2026-08-28 | overnight wave 6 product-polish | subagent | n/a | structured QA gate-report details (gateReport.ts helpers + GateReports render by severity), delivery/notify events in build log, five-gate smoke coverage, post-fix waiver count + natural-sort acceptance; merged clean, no conflicts |
| 2026-08-28 | overnight wave 6 (merge) | subagent | n/a | 3 branches merged --no-ff in order, all clean (no conflicts; migration 0010 modified not renumbered); lint/build/smoke/synth green, e2e 11 green, tests 854 → 860 → 866 |
| 2026-08-28 | overnight wave 6 audit + security | ~2.18M | ~30k | 28-agent workflow: audited the night's fixes (6 confirmed + applied incl. licence-gate realpath latent bug) + 3 streams merged (866 tests, e2e 11); then fixed a gate-details info-leak on GET /bff/jobs/:id + order list |
| 2026-08-28 | GitHub sign-in live + App→ECS-Express pivot | ~400k | ~60k | main session: GitHub sign-in wired + live-verified on dev (App perms, callback, expired-link polish); App Runner → ECS Express Mode decision (region + build via CodeBuild→ECR); GitHub delivery via App installation tokens (verified — mints tokens, admin+contents perms); ECS Express deploy client (subagent, 872 tests) replacing App Runner |
| 2026-08-28 | main session: human e2e + delivery salvage | ~600k | ~90k | live Stripe test-mode (card+Klarna, self-MoR, tax_code, webhook), family-hub #2 salvaged from a false-positive review-gate failure (no 12M rerun) + delivered live (repo+URL, PIN rate-limit fix); found 5 wave-7 items; gh installed+authed; permission rules |
| 2026-08-28 | ultracode wave 7 (build+verify) | ~1.66M | n/a | 5 streams // isolated worktrees → 15-skeptic adversarial verify → fix → gated merge: billing cost(), review-gate accuracy, delivery robustness, M9 liveness, repo hygiene; 930 tests |
| 2026-08-28 | ultracode wave 8 (build+verify+fix) | ~1.68M | n/a | 4 streams: @mf/org (vend+fenced deprovision), env-manifest, worker efficiency, approve-before-deliver; verify caught platform-wide teardown + silent secret injection; 1026 tests |
| 2026-08-28 | ultracode wave 9 (build+verify+fix) | ~1.70M | n/a | 4 streams: infra/org CDK (Customers OU+SCP), platform qa env, org onboarding+lifecycle+Customer tag, real pre-delivery approval hold; verify caught budget-killed approval hang + fence truncation; migration collision renumbered; 1075 tests |
| 2026-08-29 | ultracode wave 10 (build+verify) | ~1.01M | n/a | 3 streams: infra-qa completion, per-order service registry + resume, resident iteration-brief foundation; verify caught a customer secret-env leak (deployedService.config) + fail-open resume; 1116 tests |
| 2026-08-29 | NOTE | — | — | Run `/cost` and paste the authoritative main-session figure here — the ~ values above are observed subagent tokens per workflow, not the /cost total. Waves 7–10 subagent total ≈ 6.05M. |
| 2026-08-30 | dev real build #1 (guestbook) | 2.63M | ~250k | first real build on freshly-deployed dev via admin direct-start (payment bypass): plan→workers→5 gates green (verify/acceptance-tests/review/licence/acceptance-check), acceptance 4/4 with evidence, repo+bundle+ECR image delivered. Live deploy hit a real IAM gap (job role lacked ecs:TagResource for the wave-9 Customer tag) → fixed (infra eb2584b) + sim-validated + resources-dev redeployed. Guestbook salvaged live from the ECR image (needed AUTH_JWKS_URL/ISSUER/AUDIENCE env the delivery feeds from previewAuth): https://mf-1275f27023bf44ff81fee2ddf3b85f5c.ecs.eu-north-1.on.aws — POST/GET /guestbook verified 201/200. Follow-ups: delivered repo committed .git-broken/.git.bak (hygiene); SPA <title> left as "Template" |
| 2026-08-30 | per-job real cost + editable model prices; CI green + OIDC deploy to dev; plugins | see /cost | — | (1) billing: harness tags usage per model → jobs.usage/cost_usd (migration 0018), append-only `model_prices` table priced at order creation, admin GET/POST /bff/admin/model-prices + price panel, Cost column/totals; deployed mf-dev (60562ff). (2) CI/CD: first-ever green CI + first CI deploy to dev — `npm ci --prefix templates/web` in ci.yml, infra `github-deploy` stack (GitHub OIDC provider + `mf-github-deploy` role, trusts classic AND immutable `repo:owner@id/name@id` subjects — CloudTrail-diagnosed), dev env vars/secret, qa/live manual-only, uid-1001 fixture fix, e2e temp-root rm retry (#14). (3) Incident: pre-push hook → harness e2e git leak pushed garbage to main (8b0bbe9); restored via tree-restore commit, exec.ts sandboxEnv strips GIT_* repo env. (4) Plugins at project scope (#12): typescript-lsp, security-guidance, context7, claude-md-management, playwright (disabled). Phoenix (dev evacuation) recorded as separate later scope. Paste the /cost main-session figure here. |
| 2026-08-30 | overnight wave 11 (ultracode, 10 streams) + hardening audit + Gate A remediation | ~3.56M | n/a | Authorized overnight to run then continue autonomously. Wave 11 (37 agents, 4.4M subagent tokens, 73 min): admin UX split into routed sections, Sentry wired (api+site+portal), editable pricing-tier table + admin API, per-job S3 scoping + de-spoofed jobs-failed/token-burn alarms, Trivy image scanning (CI pin was to a deleted tag from a real 2026-03-19 supply-chain compromise, GHSA-69fq-xp46-6x23 — SHA-pinned to v0.35.0 instead), M4/M9 live-verify on dev, portal/repo cleanup, M12 margin-calculator backend; org-accounts deploy-wiring investigated and correctly declined (Phoenix's scripts don't generalize to arbitrary customer accounts). 10 PRs (#37–46) merged, 3 merge conflicts resolved (PLAN.md/TODO-EXTERNAL.md, keep-both pattern). Separately, a peer session's 61-finding hardening audit (Gates A/B/C, docs/backlog/hardening-2026-08-30/) arrived uncommitted — secured it into git + published the most urgent Gate A finding as a PLAN.md warning ahead of the fuller doc landing. Then fixed 5 of Gate A's 9 must-fix findings as reviewed, CI-verified PRs (code-only, no live AWS calls): A1 (deploy.sh now fails closed on unresolved/management-account target for qa/live), F1 (RDS deletion-protection escape hatch for the first live create), B1 (deploy.sh live refuses to run without a domain configured — was previously silent), E1 (Stripe secrets now shape-validated so the `required in live` guard actually fires on a placeholder), E2 (AuthKeySeed no longer regenerates the live signing key on a transient read error). 6 PRs (#47–52) merged. Continued into a second batch (see next row) rather than stopping — see it for the rest of Gate A. |
| 2026-08-31 | overnight Gate A remediation, batch 2 | see /cost | — | User confirmed "keep going" mid-batch after a background CI-watcher got killed by an interrupt (checked in rather than assuming stop). Fixed the rest of Gate A's provisioning-script findings, all in `infra/scripts/provision-env.mjs` / `provision-account.mjs` / new `reconcile-secrets.mjs`, as reviewed CI-green PRs (#58–61, code-only, no live AWS calls — verified field names like `SubjectAlternativeNameSummaries`/`DeletedDate` via `aws <cmd> help` rather than guessing, and sanity-tested predicate logic against mock AWS response shapes since none of these scripts have a test harness): C1 (cert reuse skips terminal FAILED/VALIDATION_TIMED_OUT certs), C3 (cert reuse requires full SAN coverage), C2 (wait-timeout re-checks real status and fails loud instead of publishing a not-ISSUED ARN with exit 0), G1 (deterministic hosted-zone caller-reference + fails loud on a duplicate zone — was `Date.now()`), D1 (NS delegation write asserts the parent zone actually owns the subdomain before writing + fails loud instead of swallowing), G2 (`CreateAccount` reconciles an in-flight request via `list-create-account-status` instead of re-firing on the same one-shot root email), F2 (`reconcile-secrets.mjs`: restores any of the six placeholder secrets currently scheduled for deletion before every deploy — `RestoreSecret` is non-destructive). Judgment calls to skip: F3 (ECR/log-group RETAIN+fixed-name retry-blocking) is `live`-only and already unreachable since B1 blocks any domainless live deploy — real fix (dropping fixed physical names) risks breaking other code that references them by name, more scope than warranted; H1 and the CDK-side half of F2 (`ForceDeleteWithoutRecovery`) both need a real IAM/AWS mutation to implement or verify, deliberately not done unattended. Gate A now: 12 of 16 findings fixed (A1, B1+B2/B3 transitively, C1/C2/C3, D1, E1-Stripe, E2, F1, F2, G1, G2); open: F3, H1, E1's anthropic/github-app-key remainder (no equivalent guard to defeat, lower value), real domain/cert provisioning. Then started Gate B (security, before onboarding untrusted customers) with two more CI-green PRs (#62, #63): B1/B2 (`renderFencedSpec()` in `packages/harness/src/job/planner.ts` — every session prompt that embeds the customer spec now fences it as untrusted data, not just the gate ones; closes the specific "spec talks the review skeptic out of its own finding" injection B1 demonstrated — regenerated the e2e replay cassette fixture since it's keyed by exact system-prompt hash), A3 (the GitHub push token no longer sits in `git push`'s argv — visible at `/proc/<pid>/cmdline` with no hidepid — routed through an inline `credential.helper` referencing an env var instead; verified by running the built invocation through `git credential fill` for real, not just asserting the token's absence from argv). Judgment call: A1 (Gate B's root cause — the shared `ANTHROPIC_API_KEY` in the untrusted worker sandbox) needs terminating Anthropic auth at the egress proxy, which needs TLS interception design + live job testing to verify — a materially different risk profile than everything else fixed tonight (offline-verifiable, narrow, mechanical), so deliberately not attempted unattended; same reasoning for A2 (secret scanner), C1/C2 (proxy architecture), D1. Gate B: 2 of 9 findings fixed; Gate C untouched. **Do not onboard untrusted customers** until Gate B's A1/A2/C1 are closed. Paste the /cost main-session figure here. |
| 2026-08-31 | Gate B A1 (root cause) — user asked to try it | see /cost | — | User explicitly asked to continue into Gate B and try the A1 egress-proxy fix — overriding my own earlier "needs TLS interception + live testing, don't attempt unattended" judgment call from the previous row. Investigated first: tinyproxy is a CONNECT tunnel, can't see/inject HTTPS headers, so TLS-interception genuinely isn't the way — but every worker session spawns the whole Claude Code CLI as the sandbox uid (worker.ts's createWorkerSpawner) with ANTHROPIC_API_KEY in its own env, and the Agent SDK/CLI supports ANTHROPIC_BASE_URL + a separate ANTHROPIC_AUTH_TOKEN (verified against the SDK's own bundled source, not guessed) — so a local-only forward proxy the worker's base URL points at, holding the real key in its own closure, closes it with zero TLS tricks and zero CDK/Dockerfile changes (loopback-only, already in NO_PROXY; outbound egress on 443 already unrestricted for any uid — C1, separately tracked). Built `apps/job/src/anthropicForwardProxy.ts` + wired it into index.ts (no more `process.env.ANTHROPIC_API_KEY =`) + denied ANTHROPIC_API_KEY by name in exec.ts's sandboxEnv (defense in depth). PR #65, CI-green, no live Anthropic calls: 6 new tests run the proxy as a real local HTTP server (header injection/stripping, genuine chunk-by-chunk SSE-style streaming passthrough, upstream error passthrough, close()) against an injected upstream fetch stub; 2 existing tests updated from documenting-the-vulnerability to asserting-the-fix; full suite 1175/1175. Judgment call on scope: A2's specific exploit (worker reads the key from env, writes it into a delivered file) is now impossible — the worker never has the real value — so A2 is transitively narrowed but not marked fixed (the general secret-scanner idea still has standalone value against other leak shapes). D1 is explicitly NOT closed: the new proxy does no metering, so a worker can still `curl` it directly, bypassing the SDK's session accounting — an out-of-band spend the budget kill-switch still can't see; flagged as the natural next chokepoint to meter at. Gate B now 3 of 9 fixed (B1/B2, A3, A1); still open: A2, C1/C2, D1, tenant-isolation (STS session tagging). Still do not onboard untrusted customers — C1 (egress fence) and D1 (budget enforcement) remain open. Paste the /cost main-session figure here. |
| 2026-08-31 | due diligence + strategy /rc session | see /cost | — | Full-project due-diligence review (artifact + docs/DUE-DILIGENCE-2026-08-31.md, PR #67 incl. worktree tidy: qa deploy residue committed, teardown-smoke.mjs promoted to infra/scripts). Then a phone /rc back-and-forth with Hasse that DECIDED the pricing rethink (build = on-ramp, 600 kr/mo managed sub, tokens ×1.5 on top, AWS passthrough +20 %, ladder ≤5k), GTM (demo gallery, voucher-as-discovery, verticalize later; Gate B+C+live = funnel prereqs), Gate C direction (verify the live URL, not more proxy gates), dogfood #1 → photo PWA (S), and the M11 dev-env fence (path allowlist + escalate-don't-refuse). All in docs/backlog/strategy-2026-08-31.md + PLAN.md updates. Paste the /cost figure here. |
| 2026-09-01 | wave 12 (5 streams) + wave 13 (audit triage/fix) | see /cost | — | Wave 12 (25 agents, 3.3M subagent tokens, 87 min): Gate C delivery core (live-URL acceptance check + per-job DB provisioning), Gate C orchestrator (gate-on-merge, demo auto-retry, NULL task_arn liveness), Gate B remainder (proxy spend metering D1, deterministic delivery secret scan A2, C1 egress-fence groundwork flag-off), pricing ladder in code + sv/en site copy, M12 margin admin UI. PRs #74/#75/#77/#78/#79 + #76 (preview-resources backlog note). Two real merge conflicts resolved by hand: secret-scan and acceptance both inserted a delivery step (order is now docs→secret-scan→repo→deploy→acceptance→bundle; positional step assertions converted to name lookup), and specService gained both the pricing-tier helper and the spec-chat limiter. GitHub push protection rejected the gate-b branch (GH013, "Stripe API Key") because the new secret scanner's own test fixtures are realistic — fixtures now assembled at runtime and the branch collapsed to one commit to purge history. Wave 13 (20 agents, 1.7M tokens, 72 min): independently re-verified all 6 P0 + 12 P1 findings of the external audit (PR #73) BEFORE fixing — 13 CONFIRMED, 6 OVERSTATED, 0 wrong; notably T1's flagship claim ("no test has ever executed real SQL") is false (packages/db/test/migrations.test.ts runs real SQL, CI sets TEST_DATABASE_URL), so P0-2 shipped with a static enum-drift guard instead of blocking on an integration suite. Fixed in PRs #80/#81/#82/#83: P0-1, P0-5, P1-8, P0-3 (every delivery was shipping without THIRD-PARTY-LICENCES.md), P0-4, P1-12, P0-2 (approveBeforeDeliver order would wedge in `building` on real Postgres), P1-2, P1-6 (the portal could never render Swedish — the locale-parity test could never fail), P1-10. Deferred items + audit corrections in docs/backlog/audit-2026-08-31-followups.md. One CI flake investigated rather than blind-rerun (git clone race, pre-existing, re-run green). Paste the /cost figure here. |
