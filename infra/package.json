{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "lint": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.267.0",
    "constructs": "^10.6.0",
    "tsx": "^4.19.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "aws-cdk": "^2.1139.0",
    "typescript": "^7.0.2"
  }
}
