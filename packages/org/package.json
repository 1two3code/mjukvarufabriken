{
  "name": "@mf/org",
  "version": "0.1.0",
  "description": "AWS Organizations account vending, cross-account role assumption, and tag-driven deprovisioning (suspend/resume/teardown) — injectable AWS clients, mocked in tests",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint ./; tsgo --noemit",
    "test": "vitest run --reporter=verbose"
  },
  "imports": {
    "#/*": "./src/*"
  },
  "dependencies": {
    "@aws-sdk/client-ecr": "^3.1118.0",
    "@aws-sdk/client-organizations": "^3.1118.0",
    "@aws-sdk/client-resource-groups-tagging-api": "^3.1118.0",
    "@aws-sdk/client-s3": "^3.1118.0",
    "@aws-sdk/client-sts": "^3.1118.0",
    "zod": "^4.4.3"
  }
}
