{
  "name": "@mf/db",
  "version": "0.1.0",
  "description": "Postgres data layer (porsager postgres driver), SQL migrations and job repositories",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint; tsgo --noemit",
    "db:migrate": "node scripts/migrate.ts",
    "db:seed": "node scripts/seed-demo.ts",
    "test": "vitest run"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@mf/models": "*",
    "postgres": "^3.4.9"
  },
  "imports": {
    "#/*": "./src/*"
  }
}
